spring:
  application:
    name: consumidor
  config:
    import:
      - 'optional:configserver:http://config-server:8888'

  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

endereco:
  ip: db
